# cfb-server

### Fixes to get things to run
1. `pyenv local 3.9.17`
2. `pip3 install -r requirements.txt`
3. Dependencies may have conflicts; use `~=` instead of `==`
4. `export PYTHONPATH=.`


# NOTE:
1. Conferences are broke after realignment